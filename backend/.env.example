# Copy to .env for local dev, or set these as secrets in your hosting platform's dashboard.
# Never commit an actual .env file with real values.

OPENAI_API_KEY=sk-...
ALLOWED_ORIGINS=https://your-frontend-domain.com
