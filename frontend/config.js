// Points the frontend at the deployed backend. For local development against
// `uvicorn main:app --reload`, temporarily change this back to
// "http://localhost:8000".
window.RL_COACH_API_BASE = "https://rocket-league-ai-assistant.onrender.com";
