// Set this to your deployed backend's URL once it's live, e.g.:
//   window.RL_COACH_API_BASE = "https://rl-coach-api.onrender.com";
// Leave as localhost for local development against `uvicorn main:app --reload`.
window.RL_COACH_API_BASE = "http://localhost:8000";
